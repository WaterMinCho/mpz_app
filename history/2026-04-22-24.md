# 완료 작업 (2026-04-22 ~ 04-24)

## deploy.yml 재설계
- `up -d --build --force-recreate` 한 줄로 통합
- concurrency 추가 (같은 브랜치 연속 push 시 이전 배포 취소)
- CACHE_BUST를 .env → docker-compose build arg로 전달
- backend 상태 기반 대기 (sleep 제거 → until 루프)
- nginx 별도 restart 제거 (resolver 자동 해결)
- collectstatic --clear 권한 에러 수정

## Supabase Storage 이관
- Cloudflare R2 → Supabase Storage 코드 이관 (cloudflare/ → storage_service/)
- dev/prod 공공데이터 동기화 완료 (7,639건, 이미지 Supabase 업로드 96.8%)
- proxy-image 제거, R2 env 정리
- R2 이미지 1건 Supabase 이관 → R2 참조 0건

## Worker 컨테이너 분리
- 동기화 스케줄러를 별도 worker 컨테이너로 분리
- 배포 시 worker 재시작 안 함 (동기화 중단 방지)
- Python 기반 스케줄러 (supercronic 대체)
- 동기화 전략 3종: incremental(매일 03:00), status_sync(매주 일), full(매월 1일)

## 프론트엔드 개선
- breed+name 조합 표시 (getDisplayBreedName 유틸)
- 전문가 의견 섹션 주석 처리 (상세 + 필터)
- 배너 반응형 (aspect-ratio), 커뮤니티/상담 버튼 비활성화
- 입양/찜 탭 sticky 처리
- 뒤로가기 router.back() 통일
- 카카오 로그인 후 직전 페이지 복귀
- GPS 서울/경기 판별 수정 (경기 우선 검사 제거)
- 홈 내 주변 기본 선택 + 지역 버튼 하이라이팅 분리
- 주목받고 있는 아이들 region 필터 제거 (전체 확성기 순)
- 로그인 페이지 모바일 높이 100dvh
- HomeHeader 로고 표시 수정 (next/image → img)
- React key 중복 경고 해결 (AnimalTab)
- 카카오 프로필 이미지 *.kakaocdn.net 와일드카드
- 홈 섹션 간격 정리 (상단 mt-5, 하단 패딩 0)

## 백엔드 개선
- 이미지 업로드 자동 리사이징 (profiles 512px, 일반 1080px) + EXIF 보정
- nginx client_max_body_size + Django DATA_UPLOAD_MAX_MEMORY_SIZE 10MB
- Django Admin 500 에러 수정 (FieldListFilter Django 5 호환)
- 동의서 수정 upsert 처리
- 센터 정보수정 책임비 필수값 제외
- SyncLog 마이그레이션 추가

## 인프라
- Railway 레거시 코드/설정 전면 제거
- nginx DNS resolver 추가 (502 방지)
- CLAUDE.md 3분할 경량화 (루트/backend/frontend)
- dev/prod admin 계정 생성
- dev 센터 계정 생성 (whalstn9 / 누텔라네강아지들)
- R2 레거시 env vars 제거 (dev/prod EC2)
